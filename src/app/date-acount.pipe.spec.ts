import { DateAcountPipe } from './date-acount.pipe';

describe('DateAcountPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAcountPipe();
    expect(pipe).toBeTruthy();
  });
});
